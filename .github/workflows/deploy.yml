name: Deploy

on:
  push:
    branches:
      - main

jobs:
  run-tests:
    uses: ./.github/workflows/run-tests.yml

  deploy:
    needs: run-tests
    uses: ./.github/workflows/run-deployment.yml
    secrets: inherit
